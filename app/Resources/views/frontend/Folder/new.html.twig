{% extends 'base.html.twig' %}

{% block title %}
{% endblock %}

{% block breadcrumb %}
    <li><a href="{{ path('homepage') }}"><i class="fa fa-dashboard"></i> {{ 'title.main_folder' | trans }}</a></li>
    <li class="active">{{ 'title.create_folder' | trans }}</li>
{% endblock %}

{% block content %}
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
              <!-- general form elements -->
              <div class="box box-default">
                <div class="box-header">
                  <h3 class="box-title">{{ 'title.create_folder' | trans }}</h3>
                </div><!-- /.box-header -->
                <!-- form start -->
                {{ form_start(form) }}
                    <div class="box-body">
                        {{ form_row(form.name) }}
                        {{ form_row(form.tag) }}
                        {{ form_row(form.plainPassword) }}
                    </div><!-- /.box-body -->

                    <div class="box-footer">
                        <div class="pull-right btn-group">
                            <button type="submit" class="btn btn-default">{{ 'action.create_folder' | trans }}</button>
                            <a href="{{ path("homepage") }}" class="btn btn-default btn-flat"><i class="fa fa-ban"></i> {{ 'action.cancel' | trans }}</a>
                        </div>
                    </div>
                {{ form_end(form) }}
              </div><!-- /.box -->

                </div><!-- /.box-body -->
              </div><!-- /.box -->
            </div>
          </div>
{% endblock %}